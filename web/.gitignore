# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# The ffmpeg.wasm core is copied here from node_modules by
# scripts/copy-ffmpeg-core.mjs on install and before every build. It is a
# ~30 MB build artefact, not source — committing it would put a binary blob
# in a repo that already carries model weights.
/public/ffmpeg/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
