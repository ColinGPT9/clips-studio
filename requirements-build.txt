# Build-only dependencies — needed to PRODUCE the Windows installer, not to
# run Clips Kitty. Install these on the build machine:
#
#     pip install -r requirements-build.txt
#
# The app's own dependencies are in requirements.txt.

pyinstaller>=6.22.2
